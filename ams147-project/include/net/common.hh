#include <algorithm>
#include <numeric>
#include <cstdlib>
#include <cstdint>
#include <cassert>
#include <random>
#include <iostream>
#include <vector>
namespace zinhart
{
  typedef std::int32_t i_type;
  typedef std::uint32_t s_type;//size type
  typedef double p_type;//precision type
  typedef std::vector<i_type> i_vect;
  typedef std::vector<s_type> s_vect;//vector of size type
  typedef std::vector<p_type> p_vect;//vector of precision type
}

